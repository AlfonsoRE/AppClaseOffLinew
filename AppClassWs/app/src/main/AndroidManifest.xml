<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para conectarse a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppClassWs"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">


        <!-- >>> AÑADE ESTO (FileProvider) <<< -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- >>> FIN FileProvider <<< -->








        <activity
            android:name=".ui.theme.CalificacionesActivity"
            android:exported="false" />





        <activity android:name=".MaterialActivity" android:exported="false" />
        <activity android:name=".CuestionarioActivity" android:exported="false" />
        <activity android:name=".CuestionarioEditorActivity" android:exported="false" />

        <activity android:name=".TareaActivity" android:exported="false" />
        <activity android:name=".ContenidoActivity" android:exported="false" />
        <activity android:name=".TemaActivity" android:exported="false" />
        <activity android:name=".ClassAlumnosActivity"
            android:exported="false" />
        <activity android:name=".ClassDetailActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".AddClassActivity" />
        <activity android:name=".JoinClassActivity" />
        <activity android:name=".ImpartidasActivity" />
        <activity android:name=".InscritasActivity" />
        <activity android:name=".ClassAnunciosActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppClassWs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
